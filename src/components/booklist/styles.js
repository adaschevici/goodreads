const styles = theme => ({
  root: {
    maxWidth: '80%',
    margin: 'auto',
    marginTop: theme.spacing(3),
    marginBottom: theme.spacing(3),
  },
  table: {
    minWidth: 1020,
    fontFamily: 'Ubuntu',
  },
  tableWrapper: {
    overflowX: 'auto',
  },
})

export default styles
